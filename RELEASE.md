# Release 规则

本文档是 OBS Live Overlay 的发布依据。每次正式版或预发布版都必须完整执行以下流程，不得复用旧版本截图或跳过发布后的验证。

## 1. 发布准备

1. 确认目标分支与远端同步，工作区中没有与本次发布无关的改动。
2. 将 `package.json` 与 `package-lock.json` 的版本同步更新为目标版本。
3. 完成功能修改，并为新增或调整的行为补充自动化测试。
4. 运行 `npm run verify`，确保类型检查、测试与构建全部通过。
5. 测试结束后关闭本项目启动的所有 Node.js 进程，并检查相关监听端口已经释放。

## 2. 发布截图

每次发布必须使用目标版本构建出的当前界面，重新生成并人工检查以下 README 截图：

- `docs/images/control-console.png`
- `docs/images/queue-overlay.png`

截图使用独立的临时数据文件和无头 Chrome，禁止读取或覆盖用户的真实 Profile 数据。截图中的演示状态固定遵循以下规则：

可运行 `npm run screenshots:release` 按以下固定数据生成两张截图；脚本会在结束时关闭临时服务和无头浏览器并删除临时数据。

- 队列用户 ID 使用“用户 + 空格 + 数字”的中性编号，例如“用户 1”“用户 2”“用户 3”“用户 4”；不得使用昵称、英文名或其他虚构身份。
- Overlay 标题使用“等待队列”。
- 标题下方的副标题使用“弹幕发送排队加入队列”。
- 队列中至少展示 4 位用户，并明确展示其中一位“当前上号”，以覆盖主要队列状态。
- 控制台截图应展示队列、顶部状态、Overlay 消息和字体编辑入口；Overlay 截图应完整展示标题、副标题、队列顺序与当前上号状态。
- 截图完成后关闭预览服务和无头浏览器，确认没有本项目 Node.js 进程或监听端口残留，并删除临时截图数据。

## 3. 发布前检查

1. 人工检查两张新截图，确认文字没有截断、界面没有异常，并且内容符合上一节规范。
2. 再次运行 `npm run verify`。
3. 运行 `npm pack --dry-run`，确认发布包只包含预期文件且版本正确。
4. 检查最终差异，确认版本号、代码、测试、文档和两张截图属于同一次发布。

## 4. 提交与发布

1. 将本次发布改动提交到目标分支并推送。
2. 创建与包版本完全一致的标签，格式为 `vX.Y.Z`；预发布版本使用合法的 SemVer 后缀。
3. 推送标签，触发 `.github/workflows/release.yml`。
4. 等待 Release 工作流成功完成。工作流会验证标签与包版本、运行完整校验、生成带校验和的发布包、创建 GitHub Release，并通过 npm Trusted Publishing 发布包。

## 5. 发布后验证

1. 确认 GitHub Release 已生成，发布包和 SHA-256 文件均可下载。
2. 确认 npm 上的版本与标签一致。
3. 检查默认安装命令可以取得新版本，并验证 `obs-live-overlay --version` 输出正确。
4. 再次确认本地没有为测试或截图启动的 Node.js 进程和监听端口。
